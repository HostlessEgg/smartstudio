# Fecha: 2026-02-06

## Resumen
- Se consolidó el backend oficial (`backend/server.js`) y se avanzó en seguridad de sesión.
- Se estandarizó configuración de entorno y se añadieron límites de uploads.

## Cambios técnicos
- Auth con cookies httpOnly + CSRF en backend.
- Endpoints nuevos: `/api/auth/csrf`, `/api/auth/me`, `/api/auth/logout`.
- Frontend migrado a cookies + CSRF (sin tokens en localStorage).
- `.env.example` añadido con variables estándar y `CORS_ORIGIN`/`UPLOAD_MAX_MB`.
- Uploads con límites de tamaño y validación MIME.
- Helper de validación centralizada para payloads en `backend/server.js`.
- Tests: agregado coverage mínimo para `/api/auth/csrf` y `/api/auth/me`.
- Observabilidad básica: request-id + logging con morgan.
- Cliente: se unifica uso de axios en ToastContext (sin fetch suelto).
- Backend: dbConfig extraído a módulo `backend/lib/db.js`.
- Backend: auditoría extraída a `backend/lib/audit.js`.
- Backend: helpers de auth/CSRF extraídos a `backend/lib/auth.js`.
- Backend: helper `getConnection` usado en rutas críticas (auth/usuarios/admin) y representatives.
- Backend: shim de `mysql.createConnection` reintroducido con protección anti-recursión (usa helper DB).
- Backend: feature flags y auditoría admin migrados a `getConnection`.
- Backend: cursos/inscripciones/lessons/curriculum migrados a `getConnection`.
- Backend: assignments/submissions/quizzes/foros/cohorts/progress migrados a `getConnection`.
- Backend: admin/users/notifications y auditoría usan `getConnection`.
- Frontend: unificación de cliente HTTP con `lib/api` en páginas/componentes.
- Frontend: unificación de llamadas HTTP en `lib/api.js` (pages/components/subjects).
- Backend: representatives ahora usa `getConnection` en `/received` y `/cancel`.

## Pruebas
- npm install en backend (cookie-parser).
- npm test en backend (10/10 suites OK).
- npm install en backend (morgan).
- npm test en backend (10/10 suites OK) tras refactor DB parcial.
- npm test en backend (10/10 suites OK) tras corrección de representatives.
- npm test en backend (10/10 suites OK) con shim seguro de conexión DB.
- npm test en backend (10/10 suites OK) tras migración de cursos/inscripciones.
- npm test en backend (10/10 suites OK) tras migración completa a `getConnection`.
- npm test en backend (10/10 suites OK) tras migraciones finales (admin/users/notifications/audit).
- npm test en frontend (8/8 archivos, 12/12 tests OK).

## Pendientes / Riesgos
- Validación centralizada de payloads (schema) pendiente.
- Revisar rutas que aún usen axios directo y verificar CSRF en todos los POST.

## Próximos pasos
- Completar validación de payloads.
- Revisar flujos críticos con cookies + CSRF.
- Ajustar tests para auth por cookies.

